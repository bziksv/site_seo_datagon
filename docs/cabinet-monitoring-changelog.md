# Кабинет — мониторинг позиций (changelog)

**Модуль:** `/monitoring`, UI v2: `/monitoring-v2`  
**Config:** `cabinet.datagon.ru/config/cabinet-monitoring.php`  
**Бенчмарк UX:** [cabinet-monitoring-benchmark-seranking.md](./cabinet-monitoring-benchmark-seranking.md), [cabinet-monitoring-benchmark-topvisor.md](./cabinet-monitoring-benchmark-topvisor.md), [cabinet-monitoring-benchmark-comparison.md](./cabinet-monitoring-benchmark-comparison.md)

## 3.4.34-dev — 2026-05-28

- **Child-rows быстрее:** SQL только по месяцам среза (0/1/3/6/12), не 12 календарных месяцев; одна выборка `monitoring_keyword_prices` на все ПС вместо запроса на каждый месяц×ПС. На фронте — общий кэш HTML, прогрев первых 6 проектов после списка, prefetch по наведению на строку (120 ms).

## 3.4.33-dev — 2026-05-28

- **Пустая таблица / нет лога админа:** в `cabinet-monitoring-v2-list.js` не было закрывающей `}` у `initDataTable()` — синтаксическая ошибка, весь скрипт не выполнялся (список не грузился, панель лога не показывалась). Панель лога — видна сразу для админа (без `display: none`). Debugbar отключён на `GET /monitoring-v2` (раньше только `monitoring-v2/*` AJAX).

## 3.4.32-dev — 2026-05-28

- **«+» регионы:** без лишнего `COUNT` по БД для типичных проектов (≤12 ПС, ≤8000 слов); prefetch при наведении на «+»; строка «Загрузка статистики по регионам…» при раскрытии. Повторное открытие — из кэша (~150 ms).

## 3.4.31-dev — 2026-05-28

- Слабая иконка: PNG &lt; 1100 B (микрозаглушка gstatic) — снова в очередь `favicons/fill` (`needsRefresh`).

## 3.4.30-dev — 2026-05-28

- **1-diesel.ru и WP:** парсинг `<link rel=icon href=...>` без кавычек; поиск `favicon.ico` в HTML; для главной сначала браузерный UA (YandexBot часто таймаутит).

## 3.4.29-dev — 2026-05-28

- **favicon.ico 16×16** (kursk.himopttorg.ru и др.): `IcoFileService::extractIcon(128)` возвращал `false` — брали заглушку Google; декод с размеров 128→16, затем upscale в PNG 128×128.

## 3.4.28-dev — 2026-05-28

- **redbox.su и похожие:** второй HTTP-проход с браузерным UA; конструктор `ProjectFaviconFetcher` без type-hint `Client` (Laravel иначе подставлял Guzzle с `http_errors=true` — gstatic 404+PNG не читался). «Буквенный» stub (~1.3 KB) по-прежнему не показываем.

## 3.4.27-dev — 2026-05-28

- **Поиск в таблице:** `searching: true` (без поля DT в DOM) — иначе при `searching: false` DataTables не вызывает `ext.search` при `draw()`.

## 3.4.26-dev — 2026-05-28

- **Поиск v2:** фильтр таблицы привязан к `settings` DataTable (не `nTable.id`); debounce 220 ms; график обновляется без пересоздания (`chart.update('none')`); поиск по URL, имени и пользователям проекта.

## 3.4.25-dev — 2026-05-28

- **502 на `projects/list`:** Laravel Debugbar с `storage.enabled=false` писал профиль AJAX в заголовки (`phpdebugbar-*`) → nginx `upstream sent too big header`. Исключение `monitoring-v2/*` в `config/debugbar.php`, storage по умолчанию в файл; запас: `fastcgi_buffer_*` в `scripts/nginx-cabinet.local.conf` (перезапуск `dev-local.sh`).

## 3.4.24-dev — 2026-05-28

- Исправлен **502** на `projects/list`: убран N+1 `findDonorProject` при сборке JSON; `propagate` в try/catch, без сброса кэша внутри `remember`.

## 3.4.23-dev — 2026-05-28

- **favicon.ico:** декод через `lordelph/icofileloader` (16×16 ICO → PNG), не картинки из `/upload/iblock`.
- Порядок: `/favicon.ico` → `<link rel="icon">` → apple-touch; без og:image и баннеров.
- Донор по хосту: не «самый большой файл», а нормальная иконка (штраф &gt; 20 KB).
- Кэш списка: **s14**.

## 3.4.22-dev — 2026-05-28

- **metprof-vrn.ru и .ico:** GD не читает `.ico` — декод через PNG внутри ICO / Imagick; HTML главной снова парсится (`fetchHtml`, не лимит 12 KB).
- Порядок URL: первый **пригодный** PNG по списку (gstatic до Google s2 с буквой).

## 3.4.21-dev — 2026-05-28

- Загрузка фавиконок: **User-Agent YandexBot/3.0** (env `MONITORING_FAVICON_USER_AGENT`) — сайты с антиботом чаще отдают HTML и `/favicon.ico`.

## 3.4.20-dev — 2026-05-28

- Загрузчик фавиконок: **сначала сайт** (favicon.ico, HTML), агрегаторы (Google) — запасной; Google 128×128 с буквой больше не перебивает реальный .ico.
- Порог мелкого файла **500 B** (gstatic 16×16); буквенная плитка Google по-прежнему 1290–1345 B.
- Клик «обновить иконку» — полный `favicon_src_project_id` в `updates` для клиента.

## 3.4.19-dev — 2026-05-28

- Порог «слабой» иконки: не «&lt; 3 KB», а **буквенная заглушка Google ~1.3 KB** (1290–1345) или **&lt; 800 B**. Нормальные 1.1–2.5 KB снова показываются (jetour, proktolog, eda-doma и т.д.).
- Кэш списка: **s11**.

## 3.4.18-dev — 2026-05-28

- **Заглушки Google (~1.3 KB)** больше не считаются нормальной иконкой: донор выбирается по **размеру PNG**, не по дате; слабые файлы копируются с проекта того же домена (напр. `metplus-msk.ru` id 672 → 82).
- Быстрый `favicons/fill` **не сохраняет** буквенные PNG; для слабых — полный обход сайта.
- Кэш списка: **s10**.

## 3.4.17-dev — 2026-05-28

- **Иконки в списке:** `<img src>` — **`/storage/monitoring-favicons/{id}.png`** (статика, без auth). Маршрут `/monitoring-v2/favicon?project=` давал **302** для подзапроса картинки → буква-заглушка при `missing_favicons: 0`.
- Кэш списка: **s9**.

**Проверка:** Network → `GET /storage/monitoring-favicons/82.png` **200**; в шапке **v3.4.17-dev**.

## 3.4.16-dev — 2026-05-28

- **Иконки в таблице:** в JSON — `favicon_src_project_id` + `favicon_v`; `<img src>` собирается на клиенте из **относительного** `/monitoring-v2/favicon?project=…` (нет рассинхрона `127.0.0.1` vs `localhost`).
- Дубли домена: тот же `favicon_src_project_id`, что у донора с PNG.
- Убрана полная перерисовка DataTable после каждого `favicons/fill` — только patch строк и DOM иконок.
- Кэш списка: суффикс **s8**.

**Проверка:** http://127.0.0.1:3002/monitoring-v2 → F5 → у двух `metplus-msk.ru` одна и та же картинка; Network — `GET /monitoring-v2/favicon?project=…` **200**.

## 3.4.15-dev — 2026-05-28

- `favicon_url` в list — через `/monitoring-v2/favicon?project=` (тот же хост, что страница), не `asset(storage/…)` с другим origin.

## 3.4.14-dev — 2026-05-28

- **«Обновить»:** в `list` сразу `favicon_url` с донора того же домена; перерисовка таблицы; фоновая догрузка только для строк без иконки; прогресс «Иконки проектов».

## 3.4.13-dev — 2026-05-28

- Исправлен **500** на `favicons/fill?project_id=` — `isNewerFavicon` падал на строковом `favicon_updated_at` без cast Carbon.

## 3.4.12-dev — 2026-05-28

- **Один домен — одна иконка:** при `list` и `favicons/fill` копирование PNG с любого проекта того же хоста в портфеле; кэш списка `s5`.

## 3.4.11-dev — 2026-05-28

- **Клик по иконке** — `POST favicons/fill` с `project_id`, полная загрузка; дубли URL копируют PNG с соседнего проекта.
- **«Обновить»** — после метрик запускает догрузку пропущенных фавиконок; сброс кэша списка при сохранении иконки.

## 3.4.10-dev — 2026-05-28

- **Зависание при F5:** в таблице больше нет `favicon?host=` в `<img>` (десятки on-demand fetch); буква-заглушка → фон `favicons/fill`. `?host=` отключён.

## 3.4.9-dev — 2026-05-28

- `favicons.fill`: **2 иконки за запрос**, быстрый режим (Google/gstatic, без HTML), лимит 12 с; повтор при обрыве.

## 3.4.8-dev — 2026-05-28

- `snapshots.fill`: **1 проект за запрос**, лимит ~22 с на HTTP (без обрыва на 90 с); повтор при `status: 0`; тост при таймауте.

## 3.4.7-dev — 2026-05-28

- Очередь фоновых задач: фавиконки только после `list.ready` и завершения `snapshots.fill`; отмена отложенного таймера при новом list.
- Старый кэш списка (без `snapshots_pending`) — суффикс кэша `s4`; на клиенте пересчёт pending по пустым строкам.
- Админ-лог: новая сессия и `MonitoringV2DebugLog::clear` при открытии страницы.

## 3.4.6-dev — 2026-05-28

- Исправлено «страница не открывается» / зависание вкладки: метрики и фавиконки **последовательно** (не параллельно), лимит шагов fill, лёгкое обновление строк таблицы без полной пересборки на каждую порцию.

## 3.4.5-dev — 2026-05-28

- Админ-лог (Super Admin / admin): панель «Расширенный лог» внизу `/monitoring-v2`, серверные тайминги list/fill + клиентские события; `debug_session` в POST.
- Фавиконки без сохранённого файла: сначала `?host=`, меньше 404 в консоли на `?project=`.

## 3.4.4-dev — 2026-05-28

- Фавиконки: фоновая догрузка `POST /monitoring-v2/favicons/fill` (6 проектов за запрос), при ошибке img — повтор с `refresh=1` и fallback по `host`; в fetcher приоритет Google/gstatic.

## 3.4.3-dev — 2026-05-28

- После фоновой догрузки метрик таблица **перерисовывается целиком** (DataTables не обновлял HTML ячеек при `row().data()`).
- `pending` в fill считается по фактически пустым снимкам в БД, не «остаток очереди».

## 3.4.2-dev — 2026-05-28

- Исправлено зависание «Загрузка списка…»: **list отдаётся сразу** (без пересчёта снимков в том же HTTP).
- Метрики догружаются фоном: `POST /monitoring-v2/snapshots/fill` (3 проекта за запрос, прогресс «Метрики проектов: N / M»).
- «Обновить» — сброс кэша списка + фоновый fill с `force=1` (5 проектов за запрос).

## 3.4.1-dev — 2026-05-28

- ~~Пересчёт снимков внутри list~~ — убрано в 3.4.2 (висло на 86 проектах).

## 3.4-dev — 2026-05-27

- Таблица v2: колонка **ТОП 3** (и опционально ТОП 5); меню **«Столбцы»** рядом с переключателем вида — включение/скрытие метрик.
- Настройки столбцов **на пользователя** в БД (`monitoring_v2_user_preferences.list_columns`), `POST /monitoring-v2/preferences/list-columns`.
- Миграция: `2026_05_27_231500_create_monitoring_v2_user_preferences_table.php`.

**Проверка:** http://localhost:3002/monitoring-v2 → «Столбцы» → снять/включить ТОП 30, обновить страницу — выбор сохранён. Badge `v3.4-dev`.

## 3.3.3-dev — 2026-05-27

- Исправлено зависание: автопересчёт снимков убран с обычной загрузки (только по кнопке «Обновить» с `rebuild_snapshots`). Список 86 проектов отдаётся сразу.

## 3.3.2-dev — 2026-05-27

- Пустые ТОП/позиция/слова: v2 читает снимок `monitoring_data_table_columns_projects`; при отсутствии — автопересчёт (до 40 проектов за запрос). Кнопка «Обновить» шлёт `rebuild_snapshots=1`. CLI: `php artisan monitoring:refresh-snapshots --all`.

## 3.3.1-dev — 2026-05-27

- Выделение строк таблицы и плиток: фон + полоска слева (исправлен конфликт с zebra CSS).

## 3.3-dev — 2026-05-27

- Фавиконки проектов: поля `favicon_path`, `favicon_host`, `favicon_updated_at`; PNG **128×128** в `storage/app/public/monitoring-favicons/{id}.png`; выбор лучшего источника (HTML + apple-touch + агрегаторы); сохранение при создании/смене URL.
- API списка: `favicon_url`; `GET /monitoring-v2/favicon?project={id}`.
- Прогон существующих: `php artisan monitoring:refresh-favicons --missing` (после миграции БД).

## 3.2.3-dev — 2026-05-27

- ТОП 10/30/100: наполнение ячейки по проценту (цветная полоса слева направо), читаемее доля в ТОП.

## 3.2.2-dev — 2026-05-27

- Колонка ПС: компактные иконки (как в классическом мониторинге), без красных «плашек» на весь ряд.

## 3.2.1-dev — 2026-05-27

- Фавиконки проектов: `GET /monitoring-v2/favicon?host=…` — цепочка источников (apple-touch, icon.horse, Google 128px), нормализация в PNG 64×64, кэш в `storage/app/monitoring-favicons/`.

## 3.2-dev — 2026-05-27

- ТОП в таблице: три отдельные колонки (10 / 30 / 100) вместо одной «пилюли» с серыми метками; цветовая полоска и фон по силе показателя (как Topvisor).

## 3.1.3-dev — 2026-05-27

- Колонка **ПС**: вместо бледных `badge bg-light` — цветные иконки Яндекс / Google (читаемые на светлой строке).

## 3.1.2-dev — 2026-05-27

- Ячейка проекта: фавиконка домена (Google s2, fallback на букву), читаемые цвета ссылки (домен серый, название тёмное).

## 3.1.1-dev — 2026-05-27

- Контраст UI: тёмная шапка таблицы, жирные ТОП 10/30/100, насыщенные дельты, иконки проектов на акцентном фоне, тени у панелей (меньше «тусклого серого»).

## 3.1-dev — 2026-05-27

**Полная перекомпоновка страницы** (не «виджет в карточке»):

- **Серый фон** и `padding: 0` у body карточки — два отдельных блока на всю ширину.
- **Блок «Портфель позиций»** — 5 KPI в ряд + крупный график (режимы Лидеры / Распределение / Средние ТОП).
- **Блок «Все проекты»** — свой заголовок, toolbar, **таблица с фиксированным thead** (Проект · ТОП 10/30/100 · Позиция · … · Действия), без вложенной card-in-card.
- Метка **v3.1-dev** в правом верхнем углу страницы (`data-ui-build="3.1"`).

Удалены: серая «простыня» lead, дублирующий поиск DataTables.

**Проверка:** Cmd+Shift+R → виден бейдж `v3.1-dev`, серый фон, две белые панели.

## 3.0-dev — 2026-05-27

Редизайн `/monitoring-v2` по бенчмаркам [SE Ranking](./cabinet-monitoring-benchmark-seranking.md) и [Topvisor](./cabinet-monitoring-benchmark-topvisor.md). Сводка: [cabinet-monitoring-benchmark-comparison.md](./cabinet-monitoring-benchmark-comparison.md).

**Портфель (SER):** один крупный график с режимами «Лидеры / Распределение / Средние ТОП»; боковая колонка KPI (проекты, ср. ТОП‑10, ср. позиция, слова, слабые); переключатель метрик лидеров (ТОП‑10 / ТОП‑30 / позиция).

**Таблица (Topvisor + SER):** колонка «Проект» (иконка + домен + ссылка); **ТОП 10/30/100** в одной ячейке с дельтами; ср. позиция; быстрые кнопки «Позиции» и «Регионы»; меню действий.

**Плитки:** крупные KPI (позиция, ТОП‑10, слова), полоска ТОП с дельтами, те же быстрые действия; сетка до 3 колонок на широком экране.

**Прочее:** кнопка «Обновить» (сброс кэша списка); бейдж числа выбранных; дельты `diff_top*` в JSON; исправлен зависший режим плиток (2.9.1).

**Проверка:** http://localhost:3002/monitoring-v2 — Cmd+Shift+R.

## 2.9.1-dev — 2026-05-27

- **Исправлено:** режим «Плитки» зависал на «Загрузка 0 / N» — в `ingestAll` остались ссылки на удалённые переменные (`aggregateWords` и др.), в strict mode падал JS до отрисовки карточек.

**Проверка:** `/monitoring-v2` → «Плитки» → список доходит до N/N, графики заполняются.

## 2.9-dev — 2026-05-27

- UX по бенчмаркам SER/Topvisor: **таблица по умолчанию**; на карточках — компактная полоска **ТОП 10/30/100**; на дашборде — переключатель метрик графика лидеров (ТОП‑10, ТОП‑30, средняя позиция).
- Тексты подсказок в `ru.json` обновлены под логику «хаб = дайджест, детали в проекте».

**Проверка:** http://localhost:3002/monitoring-v2 → таблица; плитки → полоска ТОП; дашборд → кнопки метрик.

## 2.8-dev — 2026-05-27

- Вместо блока «86 / 86 загружено» — **дашборд Chart.js**: распределение по ТОП‑10, топ‑10 проектов, средние ТОП3/5/10/30; 4 цифры (проекты, средний ТОП‑10, слова, слабые &lt;30%). Графики пересчитываются при поиске/фильтре.

## 2.7-dev — 2026-05-27

- Палитра v2 приведена к `--cabinet-primary`: убран яркий `btn-info` (бирюза) у меню карточки, приглушены KPI/метрики/дельты ТОП, блоки регионов без кислотного `card-success`.

## 2.6-dev — 2026-05-27

- Переключатель **«Плитки / Таблица»** на `/monitoring-v2`: таблица DataTables на том же JSON, что карточки — сортировка по ТОП‑10, 30, словам и др.; режим в `localStorage`.
- Раскрытие регионов в таблице — как в классике (`+` / child-rows).

**Проверка:** http://localhost:3002/monitoring-v2 → «Таблица» → клик по заголовку «10 %»; повторно «Плитки».

- Исправлено: падение `tableFilterFn` при первом открытии таблицы (фильтр вызывался до готовности `dataTable`).

## 2.5-dev — 2026-05-27

- **Регионы и дни:** `MonitoringChildRowsService` — до 5 SQL по месяцам (или один при ≤120k строк), кэш HTML 10 мин; повторное раскрытие без запроса (кэш в DOM).
- **Mastered:** предзагрузка `monitoring_keyword_prices` одним запросом (раньше тысячи `value()` на каждую позицию — основная причина 30–50 с на child-rows).
- Консоль: `[monitoring-v2] child-rows <id> … ms` для замера.

**Проверка:** `/monitoring-v2` → «Регионы и дни» на проекте с несколькими регионами; Network — один `child-rows/get`; второй клик (свернуть/развернуть) — без запроса.

## 2.4-dev — 2026-05-27

- Список v2 быстрее: права меню считаются **один раз** (без 86× `apply_team_permissions` / `can()`), кэш JSON 120 с (ключ: пользователь + версия + штамп снимков), сброс при обновлении снимка проекта.
- Клиент: карточки рисуются пакетами по 18 кадров; аватары — инициалы до загрузки, фото с `loading="lazy"` (исправлено: `d-none` + IntersectionObserver не подгружали картинки).
- `POST …/list?refresh=1` — принудительный сброс кэша.

**Проверка:** Network → только `monitoring-v2/projects/list` (не фильтр `users`); консоль `[monitoring-v2] list loaded in … ms`; повторное F5 &lt; 500 ms при горячем кэше.

## 2.3-dev — 2026-05-27

- Список v2: `MonitoringProjectListSerializer` — один SQL + снимки, **без 172 рендеров Blade** (было ~35 с на 86 проектах). Меню и аватары собираются в JSON и на клиенте.
- Исправлен вывод «Освоено»: процент рендерится как HTML, не как текст тегов.

## 2.2-dev — 2026-05-27

- Исправлена загрузка v2: один запрос `POST /monitoring-v2/projects/list`, метрики из кэша БД (без 30× тяжёлого `ProjectData` в одном HTTP).
- При ошибке — красный блок с текстом на русском.
- Архитектура снимков: [cabinet-monitoring-snapshots.md](./cabinet-monitoring-snapshots.md), сервис `MonitoringProjectSnapshotService`, исправлен ночной cron (раньше передавался `Collection` в `ProjectData` — снимок не обновлялся).

## 2.1-dev — 2026-05-27

- **`/monitoring-v2`** — карточный интерфейс (сетка проектов, KPI, поиск, фильтр статусов, раскрытие регионов), без DataTables. Классический `/monitoring` без изменений.
- JS: `public/js/cabinet-monitoring-v2-list.js`, стили `cabinet-monitoring-v2.css`.
- Загрузка списка: пакетами по 30 проектов (3–4 запроса вместо 86); `getProjects` считает метрики для всех проектов в пакете.

### Проверка

1. http://127.0.0.1:3002/monitoring-v2 — карточки, «Регионы и дни», создание/удаление.
2. http://127.0.0.1:3002/monitoring — таблица как раньше.

## 2.0-dev — 2026-05-27

- Пункт бокового меню **«Мониторинг позиций (новый)»** сразу под классическим мониторингом (`main_projects`, миграция `2026_05_27_190000_add_monitoring_v2_main_project_menu`).
- Страница **`/monitoring-v2`** — экспериментальный список проектов (тот же API и DataTables, что `/monitoring`).
- Вёрстка: KPI, toolbar в карточках, столбцы в dropdown, `cabinet-monitoring-v2.css`.
- Классический интерфейс **`/monitoring`** не менялся.
- Блок уроков/описания на v2 — тот же, что на `/monitoring` (`DescriptionComposer`: alias `monitoring-v2` → `monitoring`).
- Интерфейс `/monitoring-v2` — локаль `ru`, ключи в `ru.json`, DataTables и toastr на русском.

### Проверка

1. http://127.0.0.1:3002/monitoring-v2 — список проектов, создание, скрытие столбцов; сверху блок «Урок — Мониторинг позиций».
2. http://127.0.0.1:3002/monitoring — без изменений.
