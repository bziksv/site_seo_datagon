# Эталон: лендинг «Генератор паролей»

| Среда | URL |
|-------|-----|
| Локально | http://localhost:3001/generator-paroley/ |
| Прод | https://datagon.ru/generator-paroley/ |

`GeneratorParoleyLanding` · `lib/content/generator-paroley-page.ts` · демо `PasswordGeneratorDemoWidget` · логика `lib/demo/password-generator-process.ts` (клиент, без API).

Акцент: **random, длина до 50, 5 вариантов, пресеты**. Демо **без лимитов** — регистрация для сохранения с комментариями, остальных модулей и доски идей.

Проверка: `npm run verify:password-gen` (порт **3001**)
