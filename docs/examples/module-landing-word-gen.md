# Эталон: лендинг «Генератор ключевых слов»

| Среда | URL |
|-------|-----|
| Локально | http://localhost:3001/generator_slov/ |
| Прод | https://datagon.ru/generator_slov/ |

`GeneratorSlovLanding` (classic `/generator_slov-v1/`) · v2 `ModuleV2Landing` на `/generator_slov/` · `lib/content/generator-slov-page.ts` · демо `KeywordGeneratorDemoWidget` · логика `lib/demo/keyword-generator-process.ts` (клиент, без API).

Акцент: **блоки слов, перемножение, операторы Директа, длина фразы**. Демо **без лимитов** — регистрация для полного кабинета и связки модулей.

Проверка: `npm run verify:word-gen` (порт **3001**)
